package atm.business.server.dal;

import atm.business.api.model.BankUser;


/**
 * BankUserDao (Data Access Object) for retrieving BankUser data from teh
 * database.
 * 
 * @author Mike Wilson
 * 
 */
public interface BankUserDao {

	/**
	 * Returns a BankUser with the provided cardNumber. Should return null if
	 * the no BankUser exists with that cardNumber.
	 * 
	 * @param cardNumber
	 * @return
	 */
	BankUser getBankUserByCardNumber(Integer cardNumber);

}
